---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.3
  creationTimestamp: null
  name: appscodes.mycrd.k8s
spec:
  group: mycrd.k8s
  names:
    kind: AppsCode
    listKind: AppsCodeList
    plural: appscodes
    singular: appscode
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AppsCode is a specification for a AppsCode resource
        properties:
          metav1.ObjectMeta:
            type: object
          metav1.TypeMeta:
            description: TypeMeta describes an individual object in an API response
              or request with strings representing the type of the object and its
              API schema version. Structures that are versioned or persisted should
              inline TypeMeta.
            properties:
              apiVersion:
                description: 'APIVersion defines the versioned schema of this representation
                  of an object. Servers should convert recognized schemas to the latest
                  internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                type: string
              kind:
                description: 'Kind is a string value representing the REST resource
                  this object represents. Servers may infer this from the endpoint
                  the client submits requests to. Cannot be updated. In CamelCase.
                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                type: string
            type: object
          spec:
            description: AppsCodeSpec is the spec for a AppsCode Resource
            properties:
              image:
                type: string
              name:
                type: string
              port:
                format: int32
                type: integer
              replicas:
                format: int32
                type: integer
            required:
            - image
            - name
            - port
            - replicas
            type: object
          status:
            description: AppsCode Status Is the Status of the AppsCode Resources
            properties:
              availableReplicas:
                format: int32
                type: integer
            required:
            - availableReplicas
            type: object
        required:
        - metav1.TypeMeta
        - metav1.ObjectMeta
        - spec
        - status
        type: object
    served: true
    storage: true
